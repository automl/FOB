task:
  name: translation
  output_dir_name: translation
  max_epochs: 18
  max_steps: 412326
  batch_size: 64
  target_metric: val_loss
  target_metric_mode: min
engine:
  devices: 4
optimizer:
  learning_rate: 1.0e-4
