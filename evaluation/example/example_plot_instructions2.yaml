data_dirs:  # List of Paths
  - evaluation/example/experiments/mnist/adamw_baseline
  - evaluation/example/experiments/mnist/sgd_baseline
output_dir: evaluation/example
experiment_name: adamw vs sgd
output_types:
  - png
plot:
  limits: [10, 95] # by manually setting the limits we force the same one for both subfigures
  std: True
  format: "2.1"
plotstyle:
  tight_layout: False  # the title is a little bit squeezed with this
