evaluation:
  output_dir: ./plots/
  output_types:
    - pdf, png
  plot:
    std: True
    x_axis:
      - weight_decay
      - kappa_init_param
  plotstyle:
    tight_layout: False  # the title is a little bit squeezed with this
